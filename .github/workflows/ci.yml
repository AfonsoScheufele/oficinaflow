name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: oficinaflow
          POSTGRES_PASSWORD: oficinaflow
          POSTGRES_DB: oficinaflow
        ports:
          - 5436:5432
        options: >-
          --health-cmd "pg_isready -U oficinaflow"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7-alpine
        ports:
          - 6383:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10

    defaults:
      run:
        working-directory: backend

    env:
      DATABASE_URL: postgresql+psycopg://oficinaflow:oficinaflow@localhost:5436/oficinaflow
      REDIS_URL: redis://localhost:6383/0
      JWT_SECRET: ci-secret-oficinaflow-32characters!!
      WEBHOOK_DEMO_SECRET: demo-webhook-secret

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Migrate
        run: alembic upgrade head
      - name: Test
        run: pytest -q
